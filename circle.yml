general:
  build_dir: Ambassador

test:
  override:
    - xctool
      -destination 'platform=iOS Simulator,name=iPhone 6,OS=latest'
      -sdk iphonesimulator
      -project Ambassador.xcodeproj
      -scheme "Ambassador"
      run-tests AmbassadorTests

# test:
#   override:
#     - xctool
#       -destination 'platform=iOS Simulator,name=iPhone 6,OS=latest'
#       -sdk iphonesimulator
#       -project Ambassador.xcodeproj
#       -scheme "Ambassador"
#       build-tests run-tests

      
#       # -reporter plain:$CIRCLE_ARTIFACTS/xctool.log

   
